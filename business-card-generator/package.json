{
  "name": "business-card-generator",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "rm -rf output && mkdir -p output && node src/index.js",
    "cluster": "rm -rf output && mkdir -p output && node src/puppeteerCluster.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "5.2.1",
    "puppeteer-cluster": "^0.22.0",
    "puppeteer-core": "^5.2.1",
    "uuid": "8.3.0"
  }
}
